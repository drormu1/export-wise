# Database provider: sqlite | sqlserver
DB_PROVIDER=sqlite

# Path to the SQLite database file (relative to src/api/)
DB_PATH=../../data/exportwise.db

# SQL Server connection string (used when DB_PROVIDER=sqlserver)
# DB_CONNECTION_STRING=Server=localhost;Database=ExportWise;Trusted_Connection=True;
